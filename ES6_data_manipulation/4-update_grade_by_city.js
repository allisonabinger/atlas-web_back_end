export default function updateStudentGradeByCity(studentList, city, newGrades) {
  const studentByLocation = studentList.filter((students) => studentList.location === city);
  return studentByLocation.map(
  )
}
